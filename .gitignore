__pycache__/
*.pyc
.venv/
venv/
data/
*.sqlite
*.sqlite-*
config/foliant.toml
.env
quellen/

# Lokale offizielle PDF-Vorlagen für die Charakterbogen-Ausgabe
vorlagen/charakterboegen/offiziell/*.pdf

# Private DDB-Beispiel-Exporte (echte Charakterdaten; nur lokaler Prüfstand, nie ins Git)
vorlagen/charakterboegen/ddb-beispiele/
# Private Golden-Tests laufen gegen die echten PDFs/Vorlagen (echte Charakterdaten +
# urheberrechtlich geschuetzte WotC-Vorlage) - nie ins Git
tests/test_charakterbogen_ddb_golden_privat.py
tests/test_charakterbogen_pdf_golden_privat.py

# Lokale Claude-Code-Einstellungen + Auslieferungs-Archive (entpackt, redundant)
.claude/settings.local.json
foliant-dokumente.zip
foliant-mvp-geruest.zip

# macOS-Finder-Metadaten
.DS_Store
.venv-ddb/

# Test-/Build-Artefakte
.pytest_cache/
*.egg-info/
build/
dist/

# ---------------------------------------------------------------------------
# PRIVAT — aus DDB-Kaufbuechern abgeleitete Druck-Reparaturen (nicht im oeffentlichen
# Repo; privater Eigenbedarf, DDB-ToS). Der oeffentliche Code laedt sie optional und
# bleibt ohne sie voll nutzbar (nur die srd-de/CC-BY-Pipeline). Siehe SECURITY.md.
importer/frhof_reparatur.py
importer/reparatur_ddb_privat.py
tests/test_ddb_druck_privat.py
