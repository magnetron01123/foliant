# Foliant - Python-Abhaengigkeiten (MVP). EXAKT GEPINNT (SYN-P1-012): jeder Pi-Deploy
# baut das Image neu - offene Ranges zogen bei jedem Rebuild potenziell andere Versionen,
# ausgerechnet pymupdf4llm ist als umgebungsempfindlich dokumentiert (CONCEPT.md §4).
# Update = bewusst Pin anheben + `make test` + Import-Stichprobe, nie beilaeufig.
fastmcp==4.0.5           # MCP-Server. Bringt das MCP-SDK 2.x mit; `get_tools()` heisst
                         # seit 3.0 `list_tools()` und liefert eine Liste (nur Tests betroffen).
uvicorn[standard]==0.53.0  # ASGI-Server fuer den HTTP-Endpunkt (Deployment)
starlette==1.6.0         # Charakterbogen-Web-App (app/charakterbogen/web.py) - DIREKT genutzt
                         # (bislang transitiv via fastmcp), deshalb explizit gepinnt (Auftrag §12).
python-multipart==0.0.32 # Multipart-Upload fuer POST /bogen (starlette braucht es fuer Forms).
httpx==0.28.1            # dnddeutsch.de-API, Open5e (nur Import)
pymupdf4llm==1.28.2      # PDF -> Markdown (Standard, kein GPU; use_ocr=False Pflicht!)
pymupdf==1.28.2          # DDB-Widget-Extraktion (app/charakterbogen/ddb_pdf.py) - jetzt
                         # DIREKTE Abhaengigkeit (nicht nur transitiv via pymupdf4llm),
                         # deshalb explizit gepinnt (Auftrag §12). Muss zu pymupdf4llm passen.
rapidfuzz==3.14.6        # tolerante Begriffssuche (B3)
discord.py==2.7.1        # Discord-Bot (app/discord_bot/) - bringt aiohttp transitiv mit
                         # (zweiter HTTP-Stack, bewusst akzeptiert: nur der Bot nutzt ihn;
                         # Anthropic-Aufrufe bleiben auf httpx, kein anthropic-SDK)
pytest==9.1.1            # Abnahmetests T1-T12 (tests/test_abnahme.py)
# docling>=2.0           # optional: Fallback für schwierige Tabellen/Statblöcke (CPU/MLX)
# tomllib ist ab Python 3.11 in der Standardbibliothek
ocrmypdf==17.*           # OCR-Vorstufe für gescannte PDFs (laeuft nur im Pi-Container; Major gepinnt).
                         # 17 fuehrt `--mode` ein; --skip-text/--redo-ocr/--force-ocr bleiben Aliase.
