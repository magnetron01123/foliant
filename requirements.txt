# Foliant - Python-Abhaengigkeiten (MVP). EXAKT GEPINNT (SYN-P1-012): jeder Pi-Deploy
# baut das Image neu - offene Ranges zogen bei jedem Rebuild potenziell andere Versionen,
# ausgerechnet pymupdf4llm ist als umgebungsempfindlich dokumentiert (bekannte_macken).
# Update = bewusst Pin anheben + `make test` + Import-Stichprobe, nie beilaeufig.
fastmcp==2.14.7          # MCP-Server (bestätigt lauffähig)
uvicorn[standard]==0.51.0  # ASGI-Server fuer den HTTP-Endpunkt (Deployment)
httpx==0.28.1            # dnddeutsch.de-API, Open5e (nur Import)
pymupdf4llm==1.28.0      # PDF -> Markdown (Standard, kein GPU; use_ocr=False Pflicht!)
rapidfuzz==3.14.5        # tolerante Begriffssuche (B3)
pytest==9.1.1            # Abnahmetests T1-T12 (tests/test_abnahme.py)
# docling>=2.0           # optional: Fallback für schwierige Tabellen/Statblöcke (CPU/MLX)
# tomllib ist ab Python 3.11 in der Standardbibliothek
ocrmypdf==16.*           # OCR-Vorstufe für gescannte PDFs (laeuft nur im Pi-Container; Major gepinnt)
